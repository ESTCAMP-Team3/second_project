import pandas as pd
import numpy as np
import os
from datetime import datetime, timedelta
import logging
# 모델 프레임워크 추가

class StockPredictionModel:

    # 특성 데이터 준비
    # 모델 학습
    # 저장된 모델 로드

    # 예측 실행
    def predict(self, symbol, data, periods=["short", "medium", "long"]):
        predictions = {}

        return  predictions